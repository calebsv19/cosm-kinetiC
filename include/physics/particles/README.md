# `include/physics/particles/`

Contains `particles2d.h`, the declaration of the tracer particle system. Any module that spawns or updates particles should include this header while the heavy lifting stays inside `src/physics/particles/`.
