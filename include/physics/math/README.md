# `include/physics/math/`

Houses math primitives shared by every solver. `math2d.h` defines `Vec2`, clamp/lerp helpers, and other inline utilities. Additional math headers (matrices, gradients, divergence operators) will live here as we expand the physics stack.
