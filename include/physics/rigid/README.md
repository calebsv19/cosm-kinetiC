# `include/physics/rigid/`

Declares the rigid-body interfaces (`rigid2d.h`). Includes the body/world structs, shape enums, and stepping routines so the rest of the engine can interact with rigid bodies without depending on the implementation.
