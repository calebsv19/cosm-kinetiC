# `include/physics/smoke/`

Contains `smoke_field.h`, the public definition of scalar smoke volumes used for volumetric exports and rendering. Import this header when you need to query or manipulate smoke density outside of the physics module.
