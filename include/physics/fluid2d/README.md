# `include/physics/fluid2d/`

Public interface for the 2D fluid solver (`fluid2d.h`). Other modules (`scene_state`, particle systems, exporters) include this header to interact with the fluid grid without touching implementation details.
