# `src/physics/smoke/`

Scalar smoke field storage and helpers. Currently just handles allocation/clearing for density grids, but this will become the home for volumetric data structures that feed the future renderer (2D slices now, 3D volumes later).
