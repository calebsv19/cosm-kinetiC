#!/usr/bin/env bash

set -euo pipefail

RENDER_DIR="export/render_frames"
VIDEO_DIR="export/render_vid"

if [ -d "$RENDER_DIR" ]; then
    rm -f "$RENDER_DIR"/frame_*.bmp
    echo "Cleared BMP frames in $RENDER_DIR"
else
    echo "Render frame directory $RENDER_DIR does not exist."
fi
